# Copy to .env and set your token (optional)
# Used by backlog PR automation (src/scripts/backlog/manage.ts)

GITHUB_TOKEN=
# Alternative env vars also supported: GH_TOKEN or GITHUB_PAT

# Optional: force base branch used for PRs (falls back to detected remote HEAD, then master/main)
# BACKLOG_BASE=master

# Optional: enable verbose debug for PR flow (prints resolved remotes/refs and API responses)
# BACKLOG_DEBUG=1